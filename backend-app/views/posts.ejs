<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed | <%= user.username %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-100 text-zinc-800 font-sans">

    <div class="w-full min-h-screen p-4">
        <div class="w-full max-w-md mx-auto">
            
            <div class="flex justify-between items-center mb-6">
                <h3 class="font-bold text-lg">Hello, <%= user.username %>! 👋</h3>
                <a href="/logout" class="text-sm text-red-500 hover:underline">Logout</a>
            </div>
            
            <div class="bg-white rounded-xl shadow-sm border border-zinc-200 p-4 mb-8">
                <form action="/createpost" method="POST">
                    <textarea 
                        name="content"
                        placeholder="What's on your mind?"
                        required
                        class="w-full bg-zinc-50 rounded-lg border border-zinc-200 p-3 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none h-24 mb-3 placeholder:text-zinc-400"
                    ></textarea>
                    
                    <div class="flex justify-end">
                        <button class="px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-md transition-colors shadow-sm" type="submit">
                            Post
                        </button>
                    </div>
                </form>
            </div>

            <div class="flex flex-col gap-4">
                <h2 class="text-xs font-bold text-zinc-400 uppercase tracking-widest px-1">Recent Posts</h2>
                
                <% if(posts.length > 0) { %>
                    <% posts.forEach(function(post) { %>
                        <div class="bg-white rounded-xl shadow-sm border border-zinc-200 overflow-hidden">
                            
                            <div class="px-4 py-3 border-b border-zinc-100 flex items-center justify-between">
                                <div class="flex items-center gap-2">
                                    <div class="w-8 h-8 rounded-full bg-zinc-800 flex items-center justify-center text-white font-bold text-xs uppercase">
                                        <%= post.createdby.username[0] %>
                                    </div>
                                    <div>
                                        <h3 class="font-semibold text-zinc-900 text-sm">@<%= post.createdby.username %></h3>
                                        <p class="text-[10px] text-zinc-400">posted just now</p>
                                    </div>
                                </div>
                            </div>

                            <div class="px-4 py-4">
                                <p class="text-sm leading-relaxed text-zinc-700">
                                    <%= post.content %>
                                </p>
                            </div>

                            <div class="px-4 py-3 bg-zinc-50 border-t border-zinc-100 flex gap-4">
                                <button class="flex items-center gap-1.5 text-sm text-zinc-500 hover:text-red-500 transition-colors">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 fill-none stroke-current" viewBox="0 0 24 24" stroke-width="2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                                    </svg>
                                    <span><%= post.likes.length %></span>
                                </button>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="text-center py-10">
                        <p class="text-zinc-400 text-sm">No posts yet. Be the first to share something!</p>
                    </div>
                <% } %>

            </div>
        </div>
    </div>

</body>
</html>